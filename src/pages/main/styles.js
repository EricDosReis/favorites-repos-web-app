import styled from 'styled-components';

export const Container = styled.main`
  form {
    display: flex;

    input {
      flex: 1;
    }
  }

  ul {
    list-style: none;
    margin: 1.5rem 0;
    padding: 0;
  }
`;
